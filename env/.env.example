# Environment Configuration Example
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# CORE CONFIGURATION
# =============================================================================

# Environment stage (affects validation requirements)
STAGE=dev
# Options: dev, staging, alpha
# - dev: Relaxed validation, warnings allowed
# - staging: Stricter validation, some features required
# - alpha: Full production validation, all features required

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Server-side Supabase configuration (API routes)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.example.anon.key
SUPABASE_SERVICE_ROLE_KEY=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.example.service.key
# Note: Service role key is optional in dev, required in staging/alpha

# Client-side Supabase configuration (frontend)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.example.anon.key
# Note: VITE_* variables are exposed to the browser
# Note: URL and anon key should match server values for consistency

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Payment system
ENABLE_MOCK_PAYMENTS=true
# Set to false in production to use real payment processing

ENABLE_X402=false
# Set to true in alpha stage to enable HTTP 402 payment challenges

# Logging and monitoring
LOG_LEVEL=info
# Options: debug, info, warn, error

ENABLE_REQUEST_LOGGING=false
# Set to true in production for request tracking

ENABLE_ERROR_TRACKING=false
# Set to true in production for error monitoring

# User features
ENABLE_EPHEMERAL_USERS=true
# Core ephemeral user functionality

ENABLE_CHAT_ALPHA=false
# Optional chat feature (alpha testing)

# =============================================================================
# X402 PAYMENT CONFIGURATION (ALPHA STAGE ONLY)
# =============================================================================

# Required when STAGE=alpha and ENABLE_X402=true
X402_PROVIDER_URL=https://your-payment-provider.com
X402_API_KEY=your_x402_api_key_here
X402_CHAIN=base-sepolia
X402_RECEIVING_ADDRESS=0x1234567890123456789012345678901234567890

# =============================================================================
# ELEVENLABS CONFIGURATION (ALPHA STAGE ONLY)
# =============================================================================

# Required when STAGE=alpha for text-to-speech functionality
ELEVEN_API_KEY=your_elevenlabs_api_key_here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Admin access token (optional)
ADMIN_TOKEN=your_admin_token_here

# Environment warnings
ALLOW_ENV_WARNINGS=false
# Set to true to allow warnings in validation (dev only)

# =============================================================================
# STAGE-SPECIFIC REQUIREMENTS
# =============================================================================

# DEV STAGE:
# - All Supabase variables required
# - Service role key optional (warning if missing)
# - X402 and ElevenLabs optional
# - Warnings allowed with ALLOW_ENV_WARNINGS=true

# STAGING STAGE:
# - All Supabase variables required including service role key
# - X402 and ElevenLabs optional
# - No warnings allowed

# ALPHA STAGE:
# - All Supabase variables required including service role key
# - X402 configuration required (all X402_* variables)
# - ElevenLabs API key required
# - No warnings allowed

# =============================================================================
# VALIDATION NOTES
# =============================================================================

# - URLs must be valid HTTP/HTTPS URLs
# - JWT tokens must start with 'eyJ' and contain '.'
# - Hex addresses must be valid 40-character hex strings with 0x prefix
# - Server and client Supabase URLs should have matching hostnames
# - Server and client anon keys should be identical
# - Use the check script to validate: npm run check:env